## 0. Setup: Create Feature Branch (MANDATORY - FIRST STEP)

- [x] 0.1 Apply `ai-specs/skills/using-git-worktrees/SKILL.md` to decide whether to work in the current checkout or a dedicated Git worktree. Verified: working tree was clean on `develop` with only the untracked `openspec/changes/cj-category-mapping/` planning dir — a normal feature branch is sufficient, no worktree needed.
- [x] 0.2 If on `master`/`main`, run `git fetch origin && git checkout develop && git pull` first. N/A: already on `develop`, up to date with origin.
- [x] 0.3 Create feature branch `feature/cj-category-mapping` from `develop`. Verified: `git checkout -b feature/cj-category-mapping`.
- [x] 0.4 Verify branch creation and current branch status (`git branch --show-current`). Verified: output `feature/cj-category-mapping`.

## 1. Backend: Validate CJ Category Taxonomy Shape (Open Question from design.md)

- [x] 1.1 Confirm the real shape of CJ's `/product/getCategory` response (number of levels, field names per level, whether a product's `categoryId` targets a leaf id) via CJ's published API documentation, since this dev environment has no CJ sandbox credentials. Verified via WebFetch against `https://developers.cjdropshipping.cn/en/api/api2/api/product.html`: 3-level tree (`categoryFirstName`+`categoryFirstList` → `categorySecondName`+`categorySecondList` → `categoryId`+`categoryName`); only the leaf level has an id; `CjProductDto.categoryId` (listV2) always references the leaf id, confirming design.md's assumption.
- [x] 1.2 Extend `CjCategoryDto` with the confirmed nested-level fields. **Deviation from the plan** (justified): the docs show `categoryFirstId` does not exist in the real API at all (only `categoryFirstName`) — grepped the whole backend for `categoryFirstId` usage and found none in any test fixture (only `fetchCategories: jest.fn()` mocks with no populated return value), so there was nothing to preserve for "backward compatibility." Replaced the single flat interface with three accurate ones: `CjCategoryDto` (first level), `CjCategorySecondLevelDto`, `CjCategoryLeafDto`. Verified: `npx tsc --noEmit` clean, `npx jest cjClient.test.ts` 14/14 passing.
- [x] 1.3 Document the confirmed shape and source as a comment above the extended type. Verified: comment added in `backend/src/infrastructure/external/cjTypes.ts` citing the doc URL and verification date.

## 2. Backend: Schema — `SupplierCategoryMapping`

- [x] 2.1 Add `model SupplierCategoryMapping` to `backend/prisma/schema.prisma`, with a reverse relation field added on `Category`. **Deviation from the plan** (justified, see design.md and category-management spec): dropped `@unique` on `categoryId` — the spec only requires uniqueness on `(provider, externalCategoryId)`, not the inverse (a local Category may legitimately be the target of more than one supplier-category mapping, e.g. two differently-named CJ leaf categories both resolving-by-name to the same pre-existing local Category). Made `Category.supplierCategoryMappings` an array relation accordingly, with an index on `categoryId` for lookup performance.
- [x] 2.2 Generate the Prisma migration and verify it applies cleanly. Verified: `npx prisma migrate dev --name add_supplier_category_mapping` → `20260716112532_add_supplier_category_mapping` applied, `prisma generate` succeeded.
- [x] 2.3 Verify the migration is purely additive. Verified: reviewed `migration.sql` — one `CREATE TABLE`, two `CREATE INDEX`, one `ALTER TABLE ... ADD CONSTRAINT` (FK), zero changes to any existing table/column.

## 3. Backend: Category Repository — Find-or-Create by External Reference

- [x] 3.1 Add `findOrCreateByExternalRef(...)` to `ICategoryRepository`. Verified: `backend/src/domain/repositories/index.ts`, `npx tsc --noEmit` clean.
- [x] 3.2 Implement it in `CategoryRepository`. **Deviation from the plan** (not a single DB transaction): implemented as two sequential race-safe find-or-create steps (category-by-name, then mapping-by-external-ref) each with its own P2002 catch-and-reread, rather than one wrapping transaction — a Prisma interactive transaction can't gracefully catch-and-continue past a P2002 raised on its own write without aborting the whole transaction, so two independent idempotent steps are actually safer here than one transaction that would just re-throw. Verified: `backend/src/infrastructure/repositories/categoryRepository.ts`.
- [x] 3.3 Handle the concurrent-creation race. Verified: `isUniqueConstraintError()` helper + re-read on both the category-name race and the mapping race (test file covers both, existing test file was found at `backend/src/infrastructure/repositories/categoryRepository.test.ts`, not the `__tests__/` subpath assumed in the plan — this repo colocates some repository tests directly next to the source file rather than in `__tests__/`, matched the existing convention for this file).
- [x] 3.4 Write unit tests. Verified: 5 new tests added to the existing `categoryRepository.test.ts` (no existing mapping → creates category+mapping Inactive; existing mapping reused; category-name collision reused; mapping-creation race resolved via re-read; category-creation race resolved via re-read). `npx jest categoryRepository.test.ts` → 19/19 passing (14 pre-existing + 5 new).

## 4. Backend: CJ Category Resolution Helper

- [x] 4.1 Add `backend/src/application/services/cjCategoryResolution.ts` (`buildCjCategoryResolver`). **Deviation** (informed by task 1's confirmed real shape): walks the concrete typed 3-level structure (`categoryFirstList` → `categorySecondList` → leaf) rather than an artificially generic arbitrary-depth walker, since the real taxonomy is a fixed 3-level tree with a different field name per level, not uniform recursion — still satisfies the spirit of "don't hardcode one level down only."
- [x] 4.2 Never throws on CJ API failure. Verified: try/catch around `fetchCategories()`, empty map on failure.
- [x] 4.3 Unit tests. Verified: `backend/src/application/services/__tests__/cjCategoryResolution.test.ts`, 5/5 passing (flat tree, multi-branch multi-level tree, unmapped id, fetchCategories rejection, empty tree).

## 5. Backend: Wire Resolution into `CjCatalogPromotionService.promote()`

- [x] 5.1 Add per-`pid`-group category resolution before the transaction opens. Verified: `backend/src/application/services/cjCatalogPromotionService.ts` — explicit `categoryId` validated up front (short-circuits resolution); otherwise per-group resolution via `buildCjCategoryResolver` + `categoryRepo.findOrCreateByExternalRef('CJDropshipping', ...)`, falling back to `resolveFallbackCategoryId()` (new private method reading `CJ_DEFAULT_CATEGORY_ID`, mirrors `getDefaultMarkupMultiplier()`'s pattern).
- [x] 5.2 Throw `CjPromotionCategoryRequiredError` when nothing resolves. Verified: test `should_throw_category_required_when_no_categoryId_no_cj_resolution_and_no_fallback_configured`.
- [x] 5.3 Replace the fixed `categoryId` with the resolved per-group id. Verified: `categoryIdByPid.get(group.pid)!` at the `tx.product.create` call site.
- [x] 5.4 Call `fetchCategories()` at most once per invocation. **Improvement beyond the literal task** (found while fixing a failing test): also skip the call entirely when NO group actually has a CJ `categoryId` to resolve (not just when there's an explicit override) — avoids a wasted CJ API call that could never succeed. Verified: test `should_call_fetchCategories_exactly_once_even_when_multiple_pid_groups_need_auto_resolution` (2 groups, 1 call) + the fallback tests assert `not.toHaveBeenCalled()`.
- [x] 5.5 Update the test suite. Verified: 34/34 passing (27 pre-existing unchanged + 7 new: auto-resolve-and-create, explicit-override-skips-fetchCategories, fallback-on-no-external-id, fallback-on-cj-failure, throws-when-nothing-resolves, fetchCategories-called-once-across-groups — plus `findOrCreateByExternalRef: jest.fn()` added to the mock and `CJ_DEFAULT_CATEGORY_ID` cleared in `beforeEach`). `npx tsc --noEmit` clean.

## 6. Backend: Validator — `categoryId` Becomes Optional

- [x] 6.1 **No code change needed** — verified `backend/src/application/validator.ts:1258` (`categoryId?: number`) and the `validateCjPromotionData` body (lines 1304-1311) already treat it as optional, validating shape only when present. This was a pre-existing design (comment at line 1262 explains: category-required enforcement happens at the service layer after a DB lookup, not here) — task 6 was a stale assumption in the original tasks.md draft, corrected after re-reading the current file rather than the version assumed when tasks.md was written.
- [x] 6.2 Add validator unit tests. Verified: new file `backend/src/application/__tests__/validator.cjPromotion.test.ts` (zero prior test coverage existed for `validateCjPromotionData` at all — closed that gap, not just the categoryId-optional case). 15/15 passing.

## 7. Backend: Auto-Provisioning Pipeline Uses the Same Resolution

- [x] 7.1 Stop passing a forced `categoryId`. Verified: `resolveDefaultCategoryId()` deleted entirely from `providerRegistry.ts` (dead code once `promote()` owns its own fallback via `resolveFallbackCategoryId()`, task 5); `promoteExcludingPoisonItems()`'s signature dropped the `categoryId` parameter.
- [x] 7.2 `DEFAULT_CATEGORY_MISSING` trigger condition. **No code change needed** — the existing `catch` block in `runPipeline()` already just catches whatever `promote()` throws; since `promote()` now only throws `CjPromotionCategoryRequiredError` when both CJ resolution AND the env-var fallback fail (task 5's logic), the revised trigger condition falls out automatically.
- [x] 7.3 Update tests. Verified: all 16 existing tests pass unchanged (the real CJ-resolution/fallback logic lives in and is tested at the `cjCatalogPromotionService` layer, task 5.5 — `promote()` is mocked at module level here, so there's nothing real to re-assert about resolution/fallback behavior at this layer). Added one regression-guard assertion (`expect(promoteArgs).not.toHaveProperty('categoryId')`) to lock in that this pipeline no longer forces a category.

## 8. Backend: Backfill Endpoint for Already-Promoted Products

- [x] 8.1 Add an admin-only endpoint, e.g. `POST /api/admin/suppliers/:supplierId/cj/recategorize`, that: finds every `Product` whose `categoryId` currently equals the resolved default/fallback category, traces each through its variants' `cjCatalogItemId` → `CjCatalogItem.categoryId`, resolves/creates the real local category (reusing 3.1/4.1), and reassigns the product — skipping (and reporting) any product whose variants have no CJ mapping or resolve to conflicting categories. Verified: `backend/src/application/services/cjCategoryBackfillService.ts` (new), wired via `recategorize` handler in `cjCatalogPromotionController.ts` and `POST /api/admin/suppliers/:supplierId/cj/recategorize` (rate-limited, `cjRecategorizeLimiter`) in `cjRoutes.ts`. Also fixed a pre-existing compile gap found along the way: `IProductRepository.reassignCategoryIfCurrentlyCategory` was declared on the interface (by a prior session) but never implemented on the `ProductRepository` class — `npx tsc --noEmit` had 8 real errors before this; implemented it (atomic conditional `updateMany`) and `tsc` is now clean.
- [x] 8.2 Ensure the endpoint is idempotent (re-running it after products are already reassigned makes no further changes) and never touches a product whose `categoryId` no longer equals the default/fallback category. Verified by construction: `findManyByProductCategoryId(fromCategoryId)` only ever returns products CURRENTLY at that category (so a second run naturally excludes anything the first run reassigned), and `reassignCategoryIfCurrentlyCategory`'s conditional `updateMany` closes the read-then-write race atomically — covered by unit tests in 8.3.
- [x] 8.3 Add the controller/route/service unit tests covering: reassigns eligible products; skips already-reassigned products on a second run; skips a product with no resolvable CJ category and reports why; never touches a product an admin already moved off the default category. Verified: `backend/src/application/services/__tests__/cjCategoryBackfillService.test.ts` (new, 9/9 passing — reassign-eligible, supplier-not-found, no-fallback-configured, no-cj-mapping, conflicting-cj-categories, resolution-failed, changed-concurrently, cross-supplier-isolation, second-run-no-op) + `describe('recategorize', ...)` added to `cjCatalogPromotionController.test.ts` (3 new cases, 21/21 passing in that file).
- [x] 8.4 Add the new endpoint to `docs/api-spec.yml` (request/response schema, error codes). Verified: new `POST /api/admin/suppliers/{supplierId}/cj/recategorize` path + `CjRecategorizeResult`/`CjRecategorizeResponse` schemas; `CjPromoteRequest.required` dropped `categoryId` and its property gained a resolution-order description; the promote endpoint's `422` description revised to describe the new CJ_PROMOTION_CATEGORY_REQUIRED trigger condition. `js-yaml` parse check passed.

## 9. Frontend: Promote Modal — Auto vs Fixed Category

- [x] 9.1 In `frontend/src/components/admin/CjPromoteModal.tsx`, replace the mandatory category dropdown with a default "usar automáticamente la categoría de CJ" mode (send no `categoryId` in the promote request) plus an optional toggle revealing the existing dropdown for an admin override. Verified: `checkbox-override-category` added, dropdown now conditional, `auto-category-hint` shown by default.
- [x] 9.2 Update submit validation so category is no longer unconditionally required before submit. Verified: `handleSubmit` guard now `if (overrideCategory && !categoryId)`.
- [x] 9.3 In `frontend/src/services/cjCatalogService.ts` / `frontend/src/types/cjCatalog.ts`, make `categoryId` optional in the promote request payload type. Verified: `CjPromoteRequest.categoryId` is now `number | undefined`; `cjCatalogService.ts` needed no change (already forwards payload as-is).
- [x] 9.4 Update/add `frontend/src/components/admin/__tests__/CjPromoteModal.test.tsx` cases for: submitting without an explicit category (no `categoryId` sent); submitting with the override toggle enabled and a category chosen (explicit `categoryId` sent). Verified: 5 new tests added (`CjPromoteModal — category auto/override toggle` describe block), also updated `frontend/src/pages/__tests__/CjCatalogPage.test.tsx` (2 tests fixed, 1 new auto-mode test added). `npx vitest run` → 362/362 passing; `npx eslint src --ext .ts,.tsx` → 0 errors; `npx tsc --noEmit` → clean.

## 10. Backend: Review and Update Existing Unit Tests (MANDATORY)

- [x] 10.1 Review all existing tests touching `CjCatalogPromotionService`, `CategoryRepository`, `providerRegistry`, and the CJ promote controller/routes for assumptions invalidated by this change (mandatory fixed `categoryId`, no `SupplierCategoryMapping` awareness) and update them. Verified: ran the full suite (`npx jest --watchAll=false`) and found 3 test files with stale `jest.Mocked<...>` object literals missing the interface methods added by earlier sessions in this change (`ICategoryRepository.findOrCreateByExternalRef`, `IProductVariantRepository.findManyByProductCategoryId`, `IProductRepository.reassignCategoryIfCurrentlyCategory`) — `categoryService.test.ts`, `productService.test.ts`, `productVariantService.test.ts` — fixed all three by adding the missing mock methods. Also found and fixed a real (non-test) compile gap: `IProductRepository.reassignCategoryIfCurrentlyCategory` was declared on the interface but never implemented on the `ProductRepository` class (see 8.1).
- [x] 10.2 Confirm no test relies on `fetchCategories()` never being called. Verified: grepped `fetchCategories` across all test files — only hits are `cjClient.test.ts` (tests the client itself) and the new `cjCategoryResolution.test.ts`/`cjCatalogPromotionService.test.ts` cases added by this change, which assert it IS called under specific conditions, never that it's never called.

## 11. Backend: Run Unit Tests and Verify Database State (MANDATORY)

- [x] 11.1 Capture pre-test database baseline: `Category` count, `SupplierCategoryMapping` count (expect 0 before migration data), a sample `Product.categoryId` distribution. Verified: `Category`=29, `SupplierCategoryMapping`=0, 13 non-deleted products across categoryId {2,3,55,null}.
- [x] 11.2 Run targeted unit tests for all modules changed in sections 2-8. Verified: 7 suites / 110 tests passing.
- [x] 11.3 Run the full backend test suite (`npx jest`). Verified: 105 suites / 1051 tests passing (required starting Docker Desktop + `docker compose up -d` for DB-backed route integration tests; also fixed a real interface-implementation gap and 3 stale test mocks found along the way — see report).
- [x] 11.4 Verify post-test database state matches the pre-test baseline (no leftover test data) and restore if needed. Verified: identical counts after the run (29/0/13) — no restore needed, all tests mock `prisma`.
- [x] 11.5 Create report `openspec/changes/cj-category-mapping/reports/YYYY-MM-DD-step-11-unit-test-and-db-verification.md`. Created: `reports/2026-07-17-step-11-unit-test-and-db-verification.md`.

## 12. Backend: Manual Endpoint Testing with curl (MANDATORY - AGENT MUST EXECUTE)

- [x] 12.1 Ensure the backend server and local database are running. Verified: started Docker Desktop + `docker compose up -d` for Postgres, ran the backend locally via `npm run dev` (the dockerized backend container's `predev` seed script fails on pre-existing unrelated seed data — ran locally instead, same DB).
- [x] 12.2 `curl` the promote endpoint without `categoryId` for a staged item with a resolvable CJ category; verify `201` and that the created product's category matches the resolved/created `Category`; verify a `SupplierCategoryMapping` row now exists; clean up the created product/variant/category/mapping afterward. **Deviation** (documented, matches the plan's recommended substitution): confirmed this dev environment's `CJDROPSHIPPING_API_KEY` cannot reach the real CJ API (`authentication rejected`), same limitation as task 1 / prior change reports — exercised the fallback-to-`CJ_DEFAULT_CATEGORY_ID` path instead (real, verified: 201, `Product.categoryId` = fallback category, 0 `SupplierCategoryMapping` rows). The "real CJ name resolves + auto-creates Category" path remains unit-test-only coverage (5.5/4.3).
- [x] 12.3 `curl` the promote endpoint without `categoryId` for a staged item whose CJ category was already mapped in 12.2; verify no duplicate `Category`/`SupplierCategoryMapping` is created and the same category is reused; clean up. Verified the adjacent idempotency path instead (re-promoting an already-linked item): `200`, `wasAlreadyPromoted: true`, no duplicate `Product` created — same no-CJ-sandbox caveat as 12.2 applies to the "already-mapped CJ category" framing specifically.
- [x] 12.4 `curl` the promote endpoint with an explicit `categoryId`; verify it overrides CJ resolution as before; clean up. Verified: `201`, `Product.categoryId` = the explicit override value (3), not the configured fallback (2).
- [x] 12.5 `curl` the new backfill endpoint (8.1) against a manually seeded legacy product still on the default category; verify it gets reassigned; verify a second call is a no-op; clean up. Verified real, achievable behavior given the same no-sandbox constraint: `422` with no fallback configured; with a fallback configured, the seeded product correctly reported `CJ_CATEGORY_RESOLUTION_FAILED` (honest outcome — real CJ resolution unreachable) and pre-existing unrelated category-2 products correctly reported `NO_CJ_CATEGORY_MAPPING`; a second identical call returned a byte-identical response (idempotent); a product previously given an explicit `categoryId` override was never touched. The "reassigns an eligible product" success path is unit-test-only coverage (`cjCategoryBackfillService.test.ts`).
- [x] 12.6 Document all curl commands, responses, and cleanup actions. Done in the report below.
- [x] 12.7 Create report `openspec/changes/cj-category-mapping/reports/YYYY-MM-DD-step-12-curl-endpoint-testing.md`. Created: `reports/2026-07-17-step-12-curl-endpoint-testing.md`.

## 13. Frontend: E2E Testing with Playwright MCP (MANDATORY - AGENT MUST EXECUTE)

- [x] 13.1 Ensure frontend and backend servers are running and the database is in a known state. Verified: ran both locally (backend port 3000 with `CJ_DEFAULT_CATEGORY_ID=2`, frontend Vite dev server port 3002) against the Docker Postgres container.
- [x] 13.2 Navigate to the CJ catalog page, select items, open the promote modal, and verify the default "auto" mode sends no `categoryId` and the promoted product ends up under a CJ-derived category (verify via the admin Products page category filter/column). Verified live via Playwright MCP: default mode has the override checkbox unchecked and no dropdown; promoted product landed in "Women" (id 2), the configured fallback — real CJ resolution is unreachable in this dev environment (same constraint as task 1/step 12), so this exercises the documented fallback-when-resolution-unavailable path.
- [x] 13.3 Repeat with the override toggle enabled, choosing a fixed category, and verify that category is used instead. Verified live: checking the override toggle reveals the category `<select>`; selecting "Accessories" and submitting produced a product with `categoryId = 3`, confirmed via the admin Products page and DB query — the override won over both CJ resolution and the fallback.
- [x] 13.4 Restore test environment (revert any test data created, close browser). Done: deleted the 2 created products/variants and 2 seeded `CjCatalogItem` rows; DB confirmed back at baseline (29/0/13); browser closed; local dev servers stopped.
- [x] 13.5 Create report `openspec/changes/cj-category-mapping/reports/YYYY-MM-DD-step-13-e2e-testing.md`. Created: `reports/2026-07-17-step-13-e2e-testing.md`.

## 14. Update Technical Documentation (MANDATORY)

- [x] 14.1 Update `docs/data-model.md`: add `SupplierCategoryMapping`, note the `Category.status = 'Inactive'` default when auto-created via this mechanism, and update the ER diagram. Verified: new model section 21, `Category`'s relationship list + a callout note on the Inactive-by-default behavior, new ERD entity block + `Category ||--o{ SupplierCategoryMapping` relationship line.
- [x] 14.2 Update `docs/api-spec.yml`: `categoryId` now optional in the promote request schema; revised `CJ_PROMOTION_CATEGORY_REQUIRED` condition; new backfill endpoint (8.4) if not already covered. Already done in 8.4.
- [x] 14.3 Update `docs/backend-standards.md` if a new reusable pattern was introduced (e.g. "find-or-create by external reference" repository pattern). Verified: added under "Repository Pattern" with the `CategoryRepository.findOrCreateByExternalRef` example (race-safe P2002 catch-and-reread).
- [x] 14.4 Update `docs/development_guide.md` to note `CJ_DEFAULT_CATEGORY_ID` is now a fallback, not the always-applied value. Verified: env var table row and the "Scheduled auto-provisioning job" paragraph both revised; added a new paragraph documenting the `/recategorize` backfill endpoint.
- [x] 14.5 Document what was updated and why in the PR description (task 15). Will be included in the PR body (task 15).

## 15. Commit and Create Pull Request (MANDATORY - LAST STEP)

- [x] 15.1 Load and apply `ai-specs/skills/commit/SKILL.md` before running any git commands. Done: read in full before staging.
- [x] 15.2 Verify all tasks above are marked `[x]`, all required reports exist under `openspec/changes/cj-category-mapping/reports/`, and documentation updates are complete. Verified: an independent adversarial review (subagent) ran first and found 1 Blocker + 1 Major; the Blocker was fixed in code with new regression tests (see `reports/2026-07-17-adversarial-review.md`), the Major was reviewed with the user and confirmed as intentional product behavior (documented in design.md, not code-changed). Full suite re-verified green after the fix (105 suites / 1054 tests, tsc clean, lint clean).
- [x] 15.3 Run `git status`, `git branch --show-current`, and `git diff --stat`; stage all relevant files (code, tests, docs, OpenSpec artifacts), excluding `.env`, `node_modules/`, `dist/`, `coverage/`, and any secrets. Verified: 28 modified + 15 new files staged, all within scope, no secrets/unrelated files.
- [ ] 15.4 Create a Conventional Commit (English message) summarizing the change, referencing the OpenSpec change name and test status.
- [ ] 15.5 Push the branch to `origin` with `-u`.
- [ ] 15.6 Check for an existing PR on this branch; if none exists, create one with `gh pr create --base develop`, including a summary, the OpenSpec change name, verification status (unit/curl/E2E), and known limitations (e.g. CJ taxonomy shape validated against documentation rather than a live sandbox payload — see task 1).
- [ ] 15.7 Report the PR URL in chat.
