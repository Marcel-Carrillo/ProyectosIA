<div class="storefront-container storefront-section">
  <p class="storefront-hero__eyebrow">{{ page.handle }}</p>
  <h1>{{ page.title }}</h1>
  <div>{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
