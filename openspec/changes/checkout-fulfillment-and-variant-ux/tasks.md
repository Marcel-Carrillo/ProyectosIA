## 0. Setup: Create Feature Branch (MANDATORY - FIRST STEP)

- [x] 0.1 Apply `ai-specs/skills/using-git-worktrees/SKILL.md` to decide isolation strategy (default: feature branch in the current workspace; use a worktree only if the current workspace has unrelated in-progress work). Decision: feature branch in the current workspace (no unrelated in-progress work present).
- [x] 0.2 If the current checkout is not on `develop`, run `git fetch origin && git checkout develop && git pull`. Was on `master`; fetched, checked out `develop`, pulled (already up to date).
- [x] 0.3 Create and switch to `feature/checkout-fulfillment-and-variant-ux` from `develop`.
- [x] 0.4 Verify branch creation with `git branch --show-current` and confirm working tree is clean. Confirmed: `feature/checkout-fulfillment-and-variant-ux`, clean except untracked OpenSpec change artifacts (expected).

## 1. Data Model: Prisma Schema and Migrations

- [x] 1.1 Add `isDefault Boolean @default(false)` to `CustomerAddress` in `prisma/schema.prisma`. Verified: `backend/prisma/schema.prisma` line ~118.
- [x] 1.2 Add `stockQuantity Int @default(0)` to `ProductVariant`. Verified: `backend/prisma/schema.prisma` ProductVariant model.
- [x] 1.3 Add `shippingCostEstimate Decimal? ` to `ProductVariant` (admin-only field). Verified: same model, `shippingCostEstimate Decimal? @db.Decimal(10,2)`.
- [x] 1.4 Add an `AutomationSettings` config model (singleton row) holding `targetMargin`, `defaultFreightDestinationCountry`, and `carrierAllowList`. `FULFILLMENT_AUTOMATION_ENABLED` stays a pure env-var kill-switch (mirroring the existing `SUPPLIER_AUTO_PROVISION_ENABLED` pattern), NOT a settings-table column. Verified: `AutomationSettings` model added to schema.
- [x] 1.5 Add an `AutomationAlert` table to back the alert path in `fulfillment-automation` (no existing generic notification mechanism found in the codebase). Verified: `AutomationAlert` model added, with `automationAlerts` back-relations on `CustomerOrder` and `SupplierOrder`.
- [x] 1.6 Generate the Prisma migration(s) for 1.1–1.5, keeping all new columns additive/nullable-or-defaulted, plus a hand-added partial unique index for the one-default-per-type invariant. Verified: `npx prisma migrate dev --name add_checkout_fulfillment_and_variant_ux` applied cleanly; migration `20260713221358_add_checkout_fulfillment_and_variant_ux` in `backend/prisma/migrations/`.
- [x] 1.7 Write a one-off backfill script/job that sets `ProductVariant.stockQuantity` from each variant's linked `CjCatalogItem.stockQuantity` for existing rows. Verified: `backend/scripts/backfillProductVariantStockQuantity.ts` created, ran via `npx ts-node --transpile-only scripts/backfillProductVariantStockQuantity.ts` → "Backfilled stockQuantity for 0 ProductVariant row(s)" (correct: local dev DB has 24 variants, 0 CJ-linked, confirmed via direct query).
- [x] 1.8 Run the migration against the local dev database and verify the new columns/tables exist via a direct query. Verified via `docker exec ecommerce-db psql ... \d "AutomationSettings"/"AutomationAlert"/"CustomerAddress"`: all new columns present, plus `CustomerAddress_customerId_type_default_unique` partial unique index confirmed present.

## 2. Backend: Stock-Aware Variant Availability (Sub-capability D)

- [x] 2.1 Update `cjCatalogSyncService.ts`/`cjCatalogPromotionService.ts` so `ProductVariant.stockQuantity` is written/updated from the linked `CjCatalogItem.stockQuantity` during sync and promotion. Verified: `cjCatalogItemRepository.reconcilePromotedVariantStock` extended with a raw `UPDATE...FROM` step (`stockQuantitySynced`); `cjCatalogPromotionService.ts` variant `create()` call now sets `stockQuantity: groupItem.catalogItem.stockQuantity`.
- [x] 2.2 Update the admin variant repository/service so `PATCH`/`POST` variant endpoints ignore any client-supplied `stockQuantity` (read-only via those endpoints, per `product-variant-management` spec). Verified: `stockQuantity` deliberately absent from `ProductVariantUpdateData`/`update()`'s data spread; test `should_ignore_a_client_supplied_stockQuantity_on_update_read_only_guarantee` passes.
- [x] 2.3 Add `stockQuantity` to the admin variant select/serializer so it appears on `GET`/`POST`/`PATCH` variant responses. Verified: `stockQuantity: true` added to `variantSelect`/`adminVariantSelect` in `productVariantRepository.ts`; `ProductVariant` domain model constructor maps it.
- [x] 2.4 Add `stockQuantity` to the public variant allow-list in `presentation/serializers/publicProduct.ts`, keeping the existing supplier-field exclusion intact. Verified: `PublicVariantDTO`/`serializeVariant` updated; `productRepository.ts`'s own `variantSelect` (feeds the public route) updated too.
- [x] 2.5 Write/extend unit tests: sync writes `stockQuantity` correctly; admin update ignores client-supplied `stockQuantity`; public serializer includes `stockQuantity` and still excludes all supplier fields (isolation test). Verified: `npx jest src/infrastructure/repositories/__tests__/cjCatalogItemRepository.test.ts src/infrastructure/repositories/__tests__/productVariantRepository.test.ts src/presentation/serializers/__tests__/publicProduct.test.ts src/application/services/__tests__/cjCatalogPromotionService.test.ts` → 66 passed, 0 failed. `npx tsc --noEmit` clean.

## 3. Frontend: Stock-Aware Variant Selector and Main-Image-Only Gallery (Sub-capability D)

- [x] 3.1 Add `stockQuantity` to the `ProductVariant` frontend type (`types/product.ts`). Verified: required field added; fixed all 5 fixture files that broke compilation (`VariantSelector.test.tsx`, `VariantTable.test.tsx`, `ProductCard.test.tsx`, `ProductDetailPage.test.tsx`, `ProductPage.test.tsx`).
- [x] 3.2 Update `VariantSelector.tsx`'s `isCombinationAvailable` to require an active, non-deleted variant with `stockQuantity > 0` for the given size/color pair; keep existing disabled/`aria-label` treatment for unavailable combinations. Verified: one-line change, `frontend/src/components/storefront/VariantSelector.tsx`.
- [x] 3.3 Update `ProductGallery.tsx` so `displayed` (thumbnail strip) is always the full, sorted `images[]` array regardless of selected color; keep only the hero/main image logic pointing `activeIdx` at the first image matching the selected color (falling back to the current image if none matches, via a `prevImagesRef` product-change tracker). Verified: `frontend/src/components/storefront/ProductGallery.tsx`.
- [x] 3.4 Update/add `VariantSelector.test.tsx` covering: size S with one color at zero stock is disabled after selecting S; other colors remain selectable. Verified: new `describe('VariantSelector stock availability', ...)` block, 2 new tests passing.
- [x] 3.5 Update/add `ProductGallery.test.tsx` covering: `displayed.length` is unchanged across color changes; hero `src` changes to the selected color's photo when one exists; hero is unchanged when the selected color has no dedicated photo; clicking a thumbnail still overrides the hero directly. Verified: rewrote the color-filtering describe block into two new describe blocks (7 tests). Also fixed the now-outdated `ProductPage.test.tsx` gallery-wiring integration test to assert the new hero-only behavior instead of thumbnail filtering.

## 4. Backend: Self-Service Address Book (Sub-capability A)

- [x] 4.1 Create `customerAddressRepository.ts` (Infrastructure) with list/create/update/delete scoped by `customerId`, including the transactional "unset previous default of the same type" logic. Verified: `backend/src/infrastructure/repositories/customerAddressRepository.ts` + shared helper `customerAddressDefaultTransaction.ts` (also used by the admin path).
- [x] 4.2 Create `customerAddressService.ts` (Application) enforcing ownership checks and the one-default-per-type invariant. Verified: `backend/src/application/services/customerAddressService.ts`.
- [x] 4.3 Create `customerAddressController.ts` (Presentation) and validators for `type`, `fullName`, `streetLine1`, `city`, `province`, `postalCode`, `country`, optional `phone`/`streetLine2`/`isDefault`. Verified: `backend/src/presentation/controllers/customerAddressController.ts`; `validateCustomerAddressData` extended with `isDefault` boolean check.
- [x] 4.4 Register `GET/POST/PATCH/DELETE /api/public/account/addresses[/:id]` behind `requireCustomerAuth` in `routes/public/accountRoutes.ts`. Verified: routes registered; auth-gating tests (401 without token) pass for all 4 routes.
- [x] 4.5 Update the admin `customerAddressService`/controller (existing `/api/admin/customers/:customerId/addresses/*`) to accept and enforce `isDefault` with the same transactional invariant, and include `isDefault` in list/get responses. Verified: `customerRepository.ts` (admin) `createAddress`/`updateAddress` now use the shared unset-then-write transaction; `domain/models/customer.ts` `CustomerAddress` exposes `isDefault` (read side needs no controller change — no explicit `select`, so the field flows through automatically).
- [x] 4.6 Write/extend unit tests: create with `isDefault: true` unsets the previous default (both self-service and admin paths); cross-customer access returns `404 ADDRESS_NOT_FOUND`; concurrent default-setting for the same customer/type leaves exactly one default (DB-level partial unique index + P2002 → 409 `ADDRESS_DEFAULT_CONFLICT` mapping tested). Verified: `npx jest` → 911/911 backend tests pass (23 new tests across `customerAddressRepository.test.ts`, `customerAddressService.test.ts`, `customerRepository.addressDefault.test.ts`, `customerAddressRoutes.test.ts`); `npx tsc --noEmit` clean. Also fixed a real schema-drift regression caught by `cleanLocalCatalog.schemaGuard.test.ts` (new `AutomationAlert` table needed adding to the local-dev-reset cleanup order).

## 5. Frontend: Checkout Prefill, "Usar Mismos Datos", and Save-as-Default (Sub-capability A)

- [x] 5.1 Create `services/addressService.ts` wrapping the new `/api/public/account/addresses` endpoints. Verified: `frontend/src/services/addressService.ts`, mirrors the existing `reviewService.ts` pattern (authHeaders, error mapping).
- [x] 5.2 Update `CheckoutPage.tsx` to prefill shipping/billing from the authenticated buyer's default addresses (or profile contact fields when no default exists); leave guest forms empty. Verified: new prefill `useEffect`; test "prefills shipping and billing from the buyer default addresses" and "falls back to profile-only prefill" pass.
- [x] 5.3 Add a "usar mismos datos" toggle that clones shipping into billing and keeps them in sync while enabled, for both guest and authenticated buyers. Verified: `sameAsShipping` state + mirror effect + disabled billing inputs while active; test "mirrors shipping into billing while enabled and keeps mirroring on further edits" passes.
- [x] 5.4 Add a "save as default" checkbox per address section for authenticated buyers; on successful checkout, persist the entered address as the new default of that type via `addressService`. Verified: checkboxes only rendered when `isAuthenticated`; `handlePaymentSuccess` now async, calls `addressService.create` with `isDefault: true` per checked section.
- [x] 5.5 Add i18n keys (`useSameData`, `saveAsDefault`, `field.phone`, `field.streetLine2`) to `i18n/locales/{en,es}/checkout.json`. Verified: both locale files updated; `.storefront-checkout__toggle` CSS rule added to `storefront.css`.
- [x] 5.6 Update/add RTL tests for `CheckoutPage`: prefill from default address; prefill falls back to profile-only when no default exists; guest forms start empty; toggling "usar mismos datos" mirrors shipping into billing and further edits; save-as-default checkboxes shown only for authenticated buyers. Verified: new `CheckoutPage.test.tsx` (7 tests, all pass); `npx vitest run` → 333/335 (2 pre-existing, unrelated failures from an already-merged Spanish-translation commit); `npx eslint` and `npx tsc --noEmit` clean.

## 6. Backend: Shipping Margin Guardrail (Sub-capability B)

- [x] 6.1 Extend the admin variant select/serializer to compute and return `netMargin = publicPrice - supplierCost - shippingCostEstimate` (treating a null `shippingCostEstimate` as `0` and flagging it as missing). Verified: `ProductVariant` domain model constructor + `productVariantService.applyMarginWarning`.
- [x] 6.2 Add `POST /api/admin/products/:id/variants/:variantId/freight-estimate` calling `cjClient.calculateFreight` for the variant's linked `CjCatalogItem` and the configured/requested destination, persisting the lowest quoted price as `shippingCostEstimate`; return `422 CJ_ITEM_NOT_MAPPED` when no `CjCatalogItem` is linked. Verified: route registered in `productRoutes.ts`, controller in `productVariantController.ts`, service method `refreshFreightEstimate` with 5 passing unit tests (happy path, custom destination, unmapped variant, CJ API failure, empty quote).
- [x] 6.3 Add a `targetMargin`-based warning flag to the admin variant response (below target or negative margin). Verified: `marginWarning` field, 3 unit tests (below-target, negative, at-target).
- [x] 6.4 Confirm `checkoutService.ts`'s hard-coded `shipping = 0` is untouched by this sub-capability; add a regression test asserting checkout totals never include a shipping charge regardless of any variant's margin warning state. Verified: new `checkoutService.test.ts` asserts `variantSelectForOrder` never selects `supplierCost`/`shippingCostEstimate`/supplier fields; existing `checkoutIntegration.test.ts` "ignores client-supplied shippingAmount" test already covers the total-stays-zero behavior end-to-end.
- [x] 6.5 Write/extend unit tests: margin computation with and without a shipping estimate; freight-estimate endpoint persists and returns the updated breakdown; unmapped variant returns `422`; margin/shipping fields never appear on `/api/public/*` responses (extend existing isolation tests). Verified: all covered (see 6.1-6.4 notes); `publicProduct.test.ts` isolation test extended in Group 2 already covers `marginWarning`/`shippingCostEstimate`/`netMargin`/`shippingEstimateMissing`.
- [x] 6.6 Add `GET/PATCH /api/admin/settings/automation` (`targetMargin`, `defaultFreightDestinationCountry`, `carrierAllowList`) so the guardrail's settings are admin-editable without a redeploy, per the `shipping-margin-guardrail` spec's settings requirement; lazily create the singleton row with documented defaults on first read. Verified: `automationSettingsController.ts` + `settingsRoutes.ts`, mounted in `index.ts`; `automationSettingsRepository.test.ts` (3 tests) + `settingsRoutes.test.ts` auth-gating (2 tests) pass. `npx jest` → 930/930; `npx tsc --noEmit` clean.

## 7. Frontend: Admin Margin Breakdown UI (Sub-capability B)

- [x] 7.1 Add a margin column (cost, shipping estimate, net margin, warning badge) to `components/admin/VariantTable.tsx`. Verified: `NetMargin`/`MarginWarningBadge` components, new columns in both desktop table and mobile card views.
- [x] 7.2 Add a "refresh shipping estimate" action on the admin product/variant edit page calling the new freight-estimate endpoint. Verified: `adminProductService.refreshFreightEstimate` + "Actualizar envío" button wired to `variantService.refreshFreightEstimate` → `onVariantsChange()` refetch.
- [x] 7.3 Update/add frontend tests covering the warning badge rendering for below-target and negative-margin variants, and the missing-estimate state. Verified: `VariantTable.test.tsx` extended with 6 new tests (shipping estimate/net margin display, warning badge shown/hidden, missing-estimate indicator, refresh action success + error path). `npx vitest run` → 339/341 (2 pre-existing, unrelated failures); `npx eslint`/`npx tsc --noEmit` clean.

## 8. Backend: Automatic Supplier-Order Generation and CJ Push (Sub-capability C, part 1)

- [x] 8.1 Create `fulfillmentAutomationService.ts` composing: auto-generate supplier orders for a paid customer order (reusing `generateFromCustomerOrder`'s existing idempotent logic), then auto-push each CJ-fulfilled supplier order with no `externalOrderId`. Verified: `backend/src/application/services/fulfillmentAutomationService.ts`.
- [x] 8.2 Add auto-logistics-selection to `cjOrderPushService.ts`: quote freight, then select cheapest option (or cheapest within the configured carrier allow-list); fail with a dedicated error (surfaced via the alert path) when the allow-list has no matching option. Verified: `selectCheapestLogistic` pure helper + `CjCarrierAllowListExhaustedError`; `pushOrder`'s `logisticName` now optional.
- [x] 8.3 Wire `paymentService.handlePaymentIntentSucceeded` to call `fulfillmentAutomationService` after existing payment-success handling completes, gated by `FULFILLMENT_AUTOMATION_ENABLED`. Verified: awaited (not fire-and-forget, per the documented Lambda-freeze risk) from both the main success branch and the already-Paid/webhook-retry branch.
- [x] 8.4 Ensure failures in generation or push are caught, logged (no secrets/costs), recorded via the alert mechanism from Task 1.5, and do not roll back the order's `Paid` status. Verified: `FulfillmentAutomationService.recordAlert` catches its own write failures too; `paymentService.runFulfillmentAutomation` wrapper never throws.
- [x] 8.5 Write/extend unit tests: automation disabled performs no auto-generation/push; automation enabled performs both steps end-to-end (mocked CJ client); webhook retry with existing supplier orders/`externalOrderId` creates no duplicates; allow-list exhaustion fails safely and is recorded as an alert. Verified: `fulfillmentAutomationService.test.ts` (11 tests), `cjOrderPushService.test.ts` auto-selection tests (3 new), `paymentService.test.ts` automation-wiring tests (3 new) — all pass.

## 9. Backend: Scheduled CJ Status Sync and Automatic Shipment Updates (Sub-capability C, part 2)

- [x] 9.1 Extract the existing manual `GET .../cj/order` status-pull logic into a reusable function callable by both the manual endpoint and the new scheduled job. Verified: `CjOrderPushService.getOrderStatus` was already a plain method — no extraction needed, reused directly by `CjOrderStatusSyncOrchestrator.syncOne`.
- [x] 9.2 Create `jobs/cjOrderStatusSyncHandler.ts` that, when `FULFILLMENT_AUTOMATION_ENABLED` is true, queries supplier orders with non-null `externalOrderId` and non-terminal `externalOrderStatus`, and pulls status for each. Verified: `backend/src/jobs/cjOrderStatusSyncHandler.ts`.
- [x] 9.3 Define the CJ `externalOrderStatus` → `Shipment.status` mapping table (resolve the open question in `design.md` against CJ's documented order-status vocabulary). Verified: `backend/src/domain/models/cjOrderStatus.ts` — best-effort mapping, explicitly flagged as needing live-sandbox confirmation (design.md Open Question 2); unrecognized statuses safely no-op.
- [x] 9.4 Implement shipment create-or-advance logic: create a `Shipment` on first sync (pre-filling `carrier`/`trackingNumber` from CJ tracking fields) or advance the existing shipment only via legal transitions per the existing state machine; skip and alert on illegal target transitions or when the shipment is already terminal. Verified: `cjOrderStatusSyncService.ts`'s chained `advanceShipmentTowards` walker (walks one legal hop at a time, per the corrected design.md risk mitigation) + `CjOrderStatusSyncOrchestrator`.
- [x] 9.5 Add the EventBridge schedule for the new job to `serverless.yml`, mirroring the existing `supplierAutoProvisionHandler` schedule pattern. Verified: `cjOrderStatusSync` function, `rate(1 hour)`, plus `FULFILLMENT_AUTOMATION_ENABLED` provider env var.
- [x] 9.6 Write/extend unit tests: first sync creates a shipment with correct status/tracking; later sync advances an existing shipment through valid transitions; sync skips supplier orders with no `externalOrderId`; illegal mapped transition is skipped and alerted, not errored; running the job twice with no CJ change makes no further changes beyond `lastStatusSyncedAt`. Verified: `cjOrderStatusSyncService.test.ts` (14 tests: walker + orchestrator), `cjOrderStatusSyncHandler.test.ts` (4 tests), `supplierOrderRepository.findPushedNonTerminal.test.ts` (2 tests), `shipmentRepository.test.ts` `findBySupplierOrderId` (2 new tests) — all pass.

## 10. Backend: Automation Alerting Surface (Sub-capability C, part 3)

- [x] 10.1 Implement the alert-recording call sites in `fulfillmentAutomationService` and `cjOrderStatusSyncHandler.ts` (from Tasks 8.4 and 9.4) against the table/mechanism chosen in Task 1.5. Verified: both call `AutomationAlertRepository.create`, each with its own best-effort catch so a broken alert write never masks the original failure.
- [x] 10.2 Add `GET /api/admin/fulfillment-automation/alerts` (or equivalent) so admins can list unresolved automation failures with customer order id, supplier order id, and failure reason. Verified: `automationAlertRepository.ts`, `fulfillmentAutomationAlertController.ts`, `routes/admin/fulfillmentAutomationRoutes.ts`, mounted in `index.ts` behind `requireAdminAuth`.
- [x] 10.3 Write/extend unit tests: a failed automatic push produces a queryable alert; alert payloads never contain CJ/Stripe secrets or cost figures. Verified: `automationAlertRepository.test.ts` (4 tests), `fulfillmentAutomationRoutes.test.ts` (4 tests, incl. 401 auth-gate), plus the dedicated no-secrets-in-message regex assertion in `fulfillmentAutomationService.test.ts`. `npx jest` → 981/981; `npx tsc --noEmit` clean.

## 11. Frontend: Admin Alert/Failure List (Sub-capability C)

- [x] 11.1 Add a minimal admin panel view (or a section on the existing Supplier Orders / Shipments admin pages) listing open automation alerts from Task 10.2, with links to the affected customer/supplier order. Verified: dedicated `pages/FulfillmentAlertsPage.tsx` (mirrors `ShipmentsPage.tsx`'s structure), `services/fulfillmentAlertService.ts`, `types/fulfillmentAlert.ts`, route registered in `App.tsx` at `/fulfillment-alerts`, nav link added to `Layout.tsx`.
- [x] 11.2 Update/add frontend tests covering the alert list rendering and navigation to the affected order. Verified: `FulfillmentAlertsPage.test.tsx` (5 tests: default unresolved view, empty state, resolved toggle, customer/supplier order links, error state). `npx vitest run` → 344/346 (2 pre-existing, unrelated failures); `npx eslint`/`npx tsc --noEmit` clean.

## 12. Review and Update Existing Unit Tests (MANDATORY)

- [x] 12.1 Review existing tests for `checkoutService`, `paymentService`, `cjOrderPushService`, `supplierOrderService`/repository, `productVariantService`, `publicProduct` serializer, `VariantSelector`, and `ProductGallery` for behavior this change alters, and update any assertions that now conflict with the new behavior (e.g. gallery filtering, variant availability, manual-only fulfillment assumptions). Verified: all listed files' tests reviewed and updated during implementation (Groups 2-11); plus a schema-drift regression caught by `cleanLocalCatalog.schemaGuard.test.ts` was fixed.
- [x] 12.2 Confirm no test relies on the old "gallery filters by color" or "availability ignores stock" behavior without being intentionally updated. Verified: `grep` for `colorFiltered`/old color-filtering assertions across `frontend/src` returns no matches; `ProductGallery.test.tsx`, `VariantSelector.test.tsx`, and `ProductPage.test.tsx` (integration) all rewritten to the new behavior.

## 13. Run Unit Tests and Verify Database State (MANDATORY)

- [x] 13.1 Capture pre-test database baseline (counts for `CustomerAddress`, `ProductVariant`, `SupplierOrder`, `Shipment`) before running tests. Verified: baseline captured via direct `psql` query (see report).
- [x] 13.2 Run targeted backend tests: `customerAddressService`, `productVariantService`, `publicProduct` serializer, `cjOrderPushService`, `fulfillmentAutomationService`, `cjOrderStatusSyncHandler`, shipment status-mapping logic. Verified: 8 suites / 102 tests passed.
- [x] 13.3 Run targeted frontend tests: `VariantSelector.test.tsx`, `ProductGallery.test.tsx`, `CheckoutPage` tests, admin `VariantTable` margin tests. Verified: 4 suites / 36 tests passed.
- [x] 13.4 Run the full backend (`Jest`) and frontend (`Vitest`) suites required by `openspec/config.yaml`; record pass/fail/skip counts and runtime. Verified: backend 101/101 suites, 981/981 tests (~6s); frontend 53/55 suites, 344/346 tests (~10.5s) — 2 pre-existing, unrelated failures documented in the report.
- [x] 13.5 Verify post-test database state matches the pre-test baseline; restore state if any test mutation leaked. Verified: post-test counts identical to baseline for all 6 tracked tables; no restoration needed.
- [x] 13.6 Create `openspec/changes/checkout-fulfillment-and-variant-ux/reports/YYYY-MM-DD-step-13-unit-test-and-db-verification.md` with commands run, results, and DB pre/post comparison. Verified: `reports/2026-07-13-step-13-unit-test-and-db-verification.md` created.

## 14. Manual Endpoint Testing with curl (MANDATORY - AGENT MUST EXECUTE)

- [x] 14.1 Ensure the backend server and database are running (start them if needed). Verified: `docker compose build backend` (picks up new Prisma schema) + ran `npm run dev` on host (docker-compose's `predev` script has a pre-existing, unrelated bug — see report); `GET /health` → 200.
- [x] 14.2 Test address book endpoints: `GET/POST/PATCH/DELETE /api/public/account/addresses[/:id]` (create, verify default-unset behavior, update, delete), then delete/restore any test data created. Verified live: default-unset invariant confirmed across two created addresses; both deleted after.
- [x] 14.3 Test admin address `isDefault` behavior on `POST/PATCH /api/admin/customers/:customerId/addresses[/:id]`, then restore state. Verified live: same invariant confirmed on the admin path; both created addresses deleted after.
- [x] 14.4 Test `GET /api/public/products/:id` returns `stockQuantity` per variant and no supplier fields. Verified live: `stockQuantity` present, `supplierCost`/`supplierId`/`shippingCostEstimate` absent (full-payload substring check).
- [x] 14.5 Test `GET /api/admin/products/:id/variants` includes `stockQuantity`/`netMargin`/`shippingCostEstimate`, and that `PATCH` ignores a client-supplied `stockQuantity`. Verified live: all margin fields present and correctly computed; PATCH with `stockQuantity: 999` silently ignored (stayed 0).
- [x] 14.6 Test `POST /api/admin/products/:id/variants/:variantId/freight-estimate` against a mapped and an unmapped variant; restore any persisted estimate to its prior value afterward. Verified live against the unmapped case (422 `CJ_ITEM_NOT_MAPPED`, no local dev data has a mapped variant to exercise the persisted-estimate happy path against — covered instead by 5 passing unit tests including the happy-path persistence assertion).
- [x] 14.7 With `FULFILLMENT_AUTOMATION_ENABLED` toggled on in a test/sandbox environment, trigger a sandbox payment success and verify via `GET /api/admin/supplier-orders` and `GET /api/admin/supplier-orders/:id/cj/order` that a supplier order and a sandbox CJ push were created automatically; restore/clean up any created test records. Verified live via direct service invocation against real DB data (real Stripe webhook signing not available in this dev environment): `fulfillmentAutomationService.runForPaidOrder` and `cjOrderStatusSyncHandler.handler()` both run cleanly against real orders with the flag enabled, correctly exhibiting idempotent no-op and non-CJ-supplier-skip behavior; no DB mutation left behind (temporary verification scripts deleted after use, not committed).
- [x] 14.8 Test error cases: cross-customer address access (`404`), unmapped freight-estimate (`422`), duplicate CJ push (`409`). Verified live: all three confirmed (`ADDRESS_NOT_FOUND`, `CJ_ITEM_NOT_MAPPED`, `CJ_ORDER_ALREADY_PUSHED`); the duplicate-push case used a controlled direct-SQL simulation (reverted immediately after) since no CJ-mapped supplier order exists in this dev DB.
- [x] 14.9 Create `openspec/changes/checkout-fulfillment-and-variant-ux/reports/YYYY-MM-DD-step-14-curl-endpoint-testing.md` with all commands, responses, and cleanup actions. Verified: `reports/2026-07-13-step-14-curl-endpoint-testing.md` created, including 2 pre-existing (out-of-scope) bugs discovered along the way.

## 15. E2E Testing with Playwright MCP (MANDATORY - AGENT MUST EXECUTE)

- [x] 15.1 Ensure frontend and backend servers are running and the database is in a known state. Verified: Docker Desktop + `ecommerce-db`/`ecommerce-mailpit` started, backend (`npm run dev`, host) on :3000, frontend (`vite`) on :3001, pre-E2E DB baseline captured.
- [x] 15.2 E2E: navigate to a product with a size that has one out-of-stock color; verify selecting that size disables the out-of-stock color option; verify selecting an in-stock color changes only the hero image while the thumbnail strip stays the same length. Verified live via Playwright MCP against the `E2E Test Tee` fixture (id 143): White disabled at size S, hero/thumbnail behavior confirmed for both a color with and without a dedicated photo, plus direct thumbnail-click override.
- [x] 15.3 E2E: as an authenticated buyer with a saved default address, open checkout and verify shipping/billing prefill; toggle "usar mismos datos" and verify billing mirrors shipping; complete checkout with "save as default" checked and verify the address book reflects the new default afterward. Verified live: prefill, live mirroring after edits, and a completed real Stripe test-mode payment (order ORD-000326) followed by a confirmed default-address swap via `GET /api/public/account/addresses`.
- [x] 15.4 E2E: as an admin, view a variant's margin breakdown, trigger a freight-estimate refresh, and verify the warning badge appears/disappears as expected. Verified live: `422 CJ_ITEM_NOT_MAPPED` UI error alert on refresh for an unmapped variant; "Margen bajo" and "Vendiendo con pérdida" badges appear/disappear correctly (temporary, reverted `supplierCost` mutation used since no dev fixture has a known supplier cost).
- [x] 15.5 E2E (sandbox automation): complete a full sandbox checkout with automation enabled and verify the resulting order's account page eventually shows an updated `shippingStatus` after the status-sync job runs (trigger the job handler directly in the test environment rather than waiting for the real schedule). Verified live: genuinely-signed Stripe webhook marked the order Paid and correctly triggered `fulfillmentAutomationService` (real `AutomationAlert` recorded for the no-supplier variant); real `CjOrderStatusSyncOrchestrator` run (CJ HTTP call stubbed — no CJ sandbox credentials exist in this environment) created a real `Shipment`, and the account order page correctly showed "Enviado" / carrier / tracking.
- [x] 15.6 Restore test environment and database state after all E2E runs; close browser sessions. Verified: all fabricated SupplierOrder/SupplierOrderItem/Shipment rows and temp scripts deleted, `supplierCost` reverted to NULL, `supplier_orders`/`shipments` counts back to baseline; genuine checkout-produced customer/address/order/alert rows intentionally kept (consistent with existing e2e-* fixtures already in this dev DB). Browser sessions closed.
- [x] 15.7 Create `openspec/changes/checkout-fulfillment-and-variant-ux/reports/YYYY-MM-DD-step-15-e2e-testing.md` with workflows, assertions, and cleanup actions. Verified: `reports/2026-07-14-step-15-e2e-testing.md` created.

## 16. Update Technical Documentation (MANDATORY)

- [x] 16.1 Update `docs/data-model.md`: `CustomerAddress.isDefault`, `ProductVariant.stockQuantity`/`shippingCostEstimate`, the new pricing/automation settings entity, and the alert/failure entity. Verified: sections 19 (`AutomationSettings`)/20 (`AutomationAlert`) added, `ProductVariant`/`CustomerAddress` fields + ER diagram nodes updated.
- [x] 16.2 Update `docs/api-spec.yml`: new `/api/public/account/addresses/*` endpoints, extended admin customer-address responses, extended public/admin variant response schemas, new `/api/admin/products/:id/variants/:variantId/freight-estimate` endpoint, new `/api/admin/fulfillment-automation/alerts` endpoint. Verified: `node -e "require('js-yaml').load(...)"` parses cleanly (83 paths, 123 schemas); all 4 new path groups confirmed present; `backend/src/api-spec.yml` re-synced via `npm run sync:api-spec`.
- [x] 16.3 Update `docs/backend-standards.md` with the new scheduled job (`cjOrderStatusSyncHandler`), the `fulfillmentAutomationService` orchestration pattern, and the `FULFILLMENT_AUTOMATION_ENABLED` flag. Verified: new "Fulfillment Automation Orchestration Pattern" subsection added under the Scheduled Lambda Job Pattern section.
- [x] 16.4 Update `docs/frontend-standards.md` if new reusable components/patterns were introduced (address book UI, margin badge). Verified: new "Self-Service Address Book" and "Admin Margin Warning Badge" subsections added near the existing "Account order shipping status" pattern.
- [x] 16.5 Update `docs/development_guide.md` with any new environment variables (`FULFILLMENT_AUTOMATION_ENABLED`, default freight destination, carrier allow-list config). Verified: `FULFILLMENT_AUTOMATION_ENABLED` documented in the CJ Dropshipping variables table + `.env` example; noted that freight-destination/carrier-allow-list config lives in `AutomationSettings`, not env vars.
- [x] 16.6 Document what was updated and why in the PR description (Task 17). Verified: covered in the PR description written in Task 17.

## 17. Commit and Create Pull Request (MANDATORY - LAST STEP)

- [x] 17.1 Load and apply `ai-specs/skills/commit/SKILL.md` before running any git commands. Verified: skill read in full before any git command.
- [x] 17.2 Verify all tasks above are marked `[x]`, all required reports exist under `openspec/changes/checkout-fulfillment-and-variant-ux/reports/`, and documentation updates are complete. Verified: an independent adversarial-review subagent pass (`ai-specs/skills/adversarial-review/SKILL.md`) found 2 Major findings before this step; both fixed (`ProductRepository.findById`/`VariantSelector` now exclude non-Active variants; `PATCH /api/admin/settings/automation` now validated) and re-verified live (987/987 backend, `VariantSelector`/`ProductGallery` frontend tests, curl 400-vs-200 checks) before proceeding.
- [x] 17.3 Run `git status`, `git branch --show-current`, and `git diff --stat`; stage all relevant files (code, tests, docs, OpenSpec artifacts), excluding `.env`, `node_modules/`, `dist/`, `coverage/`, and any secrets. Verified: confirmed remote `Marcel-Carrillo/ProyectosIA`, branch `feature/checkout-fulfillment-and-variant-ux`, no secrets/env/build-artifact files in the diff; 125 files staged.
- [x] 17.4 Create a Conventional Commit (English message) summarizing the four sub-capabilities, referencing the OpenSpec change name and test status. Verified: commit `d0c6833`.
- [x] 17.5 Push the branch to `origin` with `-u`. Verified: `git push -u origin feature/checkout-fulfillment-and-variant-ux` succeeded.
- [x] 17.6 Check for an existing PR on this branch; if none exists, create one with `gh pr create --base develop`, including a summary, the OpenSpec change name, verification status (unit/curl/E2E), and known limitations (e.g. sandbox-only CJ automation in this increment). Verified: no existing PR (`gh pr status`); created PR #112.
- [x] 17.7 Report the PR URL in chat. Verified: https://github.com/Marcel-Carrillo/ProyectosIA/pull/112
