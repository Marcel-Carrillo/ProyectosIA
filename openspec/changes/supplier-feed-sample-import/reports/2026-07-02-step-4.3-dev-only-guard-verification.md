# Task 4.3 Report - Dev-Only Guard Manual Verification

- Date: 2026-07-02
- Change: supplier-feed-sample-import
- Agent: Claude (opsx:apply)

This is a standalone evidence record for task 4.3, split out after an independent adversarial review (see below) found the guard had been exercised and reported in chat during implementation, but with no artifact recorded under `reports/` — violating `docs/base-standards.md` §5/§6's evidence-based completion rule for the only safeguard preventing this destructive script from running against a shared/production database.

## Commands Executed and Results

### Case 1: `NODE_ENV=production`

```bash
cd backend
NODE_ENV=production DATABASE_URL="postgresql://u:p@localhost:5432/db" npx ts-node prisma/importSupplierFeed.ts
```

Output:
```
Refusing to run import:supplier-feed: NODE_ENV=production.
```
Exit code: `1`. No `PrismaClient` was constructed (the check runs before `new PrismaClient()` in `main()`), so no database connection was attempted.

### Case 2: Non-local `DATABASE_URL` host

```bash
cd backend
DATABASE_URL="postgresql://u:p@prod-rds.amazonaws.com:5432/db" npx ts-node prisma/importSupplierFeed.ts
```

Output:
```
Refusing to run import:supplier-feed: DATABASE_URL host "prod-rds.amazonaws.com" is not a recognized local/dev host (expected one of: localhost, 127.0.0.1, db).
```
Exit code: `1`. Same guarantee — no database connection attempted.

### Case 3 (added after the adversarial review, to prove atomicity as well): mid-transaction failure

After the adversarial review's separate finding about missing transaction wrapping was fixed (`cleanLocalCatalog` and `importSupplierFeedProducts` now run inside a single `prisma.$transaction`), this was additionally verified: a temporary fixture entry with a 200-character product name (exceeding `Product.name`'s `VarChar(150)` limit) was appended to `supplier-feed.sample.json`, and the script was re-run.

- Pre-run state: `{ products: 3, variants: 5 }`
- Run result: failed with `The provided value for the column is too long for the column's type.`, exit code `1`, after the clean step had already deleted everything and 3 of 4 fixture products had already been re-created inside the same transaction.
- Post-run state: `{ products: 3, variants: 5 }` — identical to pre-run, confirming the entire transaction (clean + partial reload) rolled back atomically rather than leaving the database partially wiped.
- The temporary fixture entry was removed and the fixture restored to its original 3-product state (`git diff --stat` confirmed no residual diff).

## Outcome

- Task 4.3 status: PASS, with evidence now recorded per `docs/base-standards.md`'s mandatory verification gate.
