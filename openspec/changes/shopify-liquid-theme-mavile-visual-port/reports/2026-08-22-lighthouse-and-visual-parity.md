# Lighthouse, a11y, visual parity

- Date: 2026-08-22
- Change: shopify-liquid-theme-mavile-visual-port

## Status

Not run. Requires `shopify theme dev` against a store with sample products and photography.

Budgets (from design): Lighthouse mobile performance ≥ 90, accessibility ≥ 95 on home/collection/product; LCP < 2.5s.

Visual parity: compare React storefront at `frontend/` vs theme preview at 360/768/1280px using `storefront-*` classes.

## Outcome

BLOCKED pending Shopify development store.
