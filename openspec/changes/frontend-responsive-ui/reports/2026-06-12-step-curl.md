# Step 9 Report - curl Endpoint Regression (frontend-only change)

- Date: 2026-06-12
- Change: frontend-responsive-ui
- Agent: Composer

## Scope

No new backend endpoints. Regression smoke only to confirm APIs unchanged after frontend CSS/markup work.

## Commands Executed

```bash
curl.exe -s -w "\nHTTP:%{http_code}" "http://localhost:3000/api/public/products?status=Active&pageSize=1"
curl.exe -s -w "\nHTTP:%{http_code}" "http://localhost:3000/api/admin/products?pageSize=1"
curl.exe -s -H "Accept: text/html" -w "\nHTTP:%{http_code}" "http://localhost:3001/catalog"
curl.exe -s -H "Accept: text/html" -w "\nHTTP:%{http_code}" "http://localhost:3001/products"
```

## Results

| Check | Status | Notes |
|-------|--------|-------|
| GET `/api/public/products?status=Active&pageSize=1` | 200 | `{ success, data: { items, total, page, pageSize } }` |
| Public payload supplier fields | PASS | No `supplierId`, `supplierReference`, `supplierCost` in response |
| GET `/api/admin/products?pageSize=1` | 200 | Admin list envelope unchanged |
| GET `/catalog` (HTML) | 200 | Storefront serves |
| GET `/products` (HTML) | 200 | Admin panel serves |

## Database

- Baseline product count from public API: **28** active products listed in total field.
- No CREATE/UPDATE/DELETE performed — no restoration needed.

## Outcome

- Step 9 status: **PASS**
- N/A for new endpoints — frontend-only change
